import { CurrentUserIconDirective } from './current-user-icon.directive';

describe('CurrentUserIconDirective', () => {
  it('should create an instance', () => {
    const directive = new CurrentUserIconDirective();
    expect(directive).toBeTruthy();
  });
});
